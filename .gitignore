# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.expo
*.expo
apps/mobile/.expo
apps/mobile/.expo-shared
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Firebase
.firebase
.firebaserc
firebase-debug.log
firestore-debug.log
ui-debug.log

.vercel

# Playwright-managed browser binaries (downloaded locally for pilot screenshots)
.playwright-browsers/
# Scratch capture diagnostics + output from the pilot screenshot pipeline
# (underscore-prefixed; the kept pipeline is capture_lib.py / capture_screens.py)
pilot-decks/capture/_*
# Office temp/lock files (e.g. an open .pptx)
~$*
# Empty/local DB dump scratch
verification/atlas_data_dump.sql
