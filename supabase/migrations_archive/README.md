This directory stores historical local-only or superseded SQL migration files
that were removed from `supabase/migrations/` to keep local CLI migration
history aligned with the live remote project.

Files in this archive are preserved for reference only.

Do not move archived files back into `supabase/migrations/` unless you
intentionally want Supabase CLI to treat them as active migration history.
